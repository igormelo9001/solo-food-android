<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.igor.solofood">

    <application
        android:allowBackup="true"
        android:icon="@drawable/solofood"
        android:label="@string/app_name"
        android:roundIcon="@drawable/solofood"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.CadastroUsuarioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.MainActivity" />
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.TelaInicialActivity" />
        <activity android:name=".controller.PedidoActivity" />
        <activity android:name=".controller.StatusHistActivity"></activity>
    </application>

</manifest>